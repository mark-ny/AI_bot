# ── Core API ──────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-jose[cryptography]==3.3.0
httpx==0.27.0
supabase==2.4.2
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1
apscheduler==3.10.4
requests==2.31.0
websockets==12.0
python-multipart==0.0.9

# ── Scientific stack ──────────────────────────────────────────
# Pinned to versions that have manylinux wheels for Python 3.11.
# If Render is still using Python 3.14, these will fail fast
# with a clear error instead of a 10-minute compile attempt.
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
xgboost==2.0.3
ta==0.11.0
